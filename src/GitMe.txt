In this sheet:
	•	Column A contains full server names (FQDN), for example: emea-weu-p-a003.emea.dialoguecloud.local
	•	Column C contains SFB server names in uppercase, for example: EMEAWEUT00B001

Compare the first part of Column A (everything before the first dot “.”) with Column C.

The comparison must:
	•	Ignore case (case-insensitive)
	•	Only compare the text before the first dot in Column A

If the names match:
	•	Write “Yes” in Column B (Match found)
	•	Write “SFB Server” in Column D (ServerGroup)

If they do not match:
	•	Leave Columns B and D blank.

Generate the correct Excel formulas for this.
