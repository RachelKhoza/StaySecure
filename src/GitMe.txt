You are helping me perform a security-focused repository investigation for an API that may have exposed sensitive data through a reporting endpoint. I only have the repositories right now. I need you to inspect this repo and produce a clear, evidence-based inventory.

Goal:
Determine whether this repo contains API code, whether it likely hosts or contributes to a service related to report.api, whether Swagger/OpenAPI exists, how the API is authenticated, where sensitive query/report logic may live, whether there are deployment signals, and what evidence suggests the service talks to a database such as Snowflake.

Instructions:
1. Search the full repository, not just obvious files.
2. Be precise. Do not guess. For every conclusion, cite the file path and relevant code snippet or config line.
3. If something is unclear, say “unclear” and explain what evidence is missing.
4. Group findings into sections with headings.
5. At the end, provide a short risk-focused summary specifically for API security testing.

Tasks:

A. Identify whether this repo contains API code
- Find all route definitions, controllers, handlers, routers, resolvers, or endpoint declarations.
- Infer the backend framework if possible (.NET, Java Spring Boot, Node/Express/NestJS, Python Flask/FastAPI/Django, GraphQL, other).
- Produce a table with:
  - HTTP method
  - path or route
  - file path
  - handler/controller name
  - authentication or authorization middleware/attribute/guard if visible
  - notes on whether the endpoint looks report-related, admin-only, internal, or public

Search broadly for patterns such as:
- Java: @RequestMapping, @GetMapping, @PostMapping, @PutMapping, @DeleteMapping, @PatchMapping, @RestController
- Node: app.get, app.post, router.get, router.post, @Controller, @Get, @Post
- Python: @app.route, @app.get, @app.post, APIRouter, path(, re_path(
- .NET: [Route(, [HttpGet], [HttpPost], [HttpPut], [HttpDelete], [HttpPatch], ControllerBase, MapGet, MapPost
- GraphQL: type Query, type Mutation, resolver

B. Find anything specifically related to reporting or report.api
- Search for strings such as:
  - report
  - reports
  - report.api
  - reporting
  - export
  - analytics
  - query
  - sql
  - statement
  - execute
  - filter
  - tenant
  - tenantId
  - customerId
  - sensitive
- Identify files, classes, functions, routes, or services that appear to generate reports, build queries, filter data, or return bulk/sensitive data.
- Highlight any code that appears to:
  - construct SQL dynamically
  - pass user input into query builders
  - map request parameters into database filters
  - bypass tenant/user scoping
  - support export/download functionality

C. Determine whether Swagger/OpenAPI exists
Check for all of the following:
1. OpenAPI/Swagger files in the repo:
- swagger.json
- swagger.yaml
- openapi.json
- openapi.yaml
- api-docs
- docs folders with spec files

2. Framework/code support for generating Swagger/OpenAPI:
- .NET: AddSwaggerGen, UseSwagger, UseSwaggerUI, OpenApi
- Java: springdoc, springfox, @OpenAPIDefinition, @Operation
- Node: swagger-ui-express, swagger-jsdoc, openapi
- Python: FastAPI autogenerated docs, swagger config, OpenAPI config
- Any annotations, decorators, generators, or packages related to Swagger/OpenAPI

3. Pipeline/build/deploy references to spec generation or publishing:
- .gitlab-ci.yml
- included CI YAML files
- build scripts
- artifact generation
- commands that export swagger/openapi/json/yaml

Output:
- State clearly whether Swagger/OpenAPI appears to exist:
  - yes, as files
  - yes, generated by app/framework
  - yes, referenced in pipeline/build
  - no clear evidence
- Provide all supporting file paths.

D. Determine authentication and authorization patterns
Find evidence of:
- JWT or bearer token auth
- OAuth/OpenID Connect
- API keys
- session auth
- custom middleware or guards
- role checks
- policy checks
- tenant checks
- claims-based authorization
- admin vs normal user separation

Output:
- Summarize how a normal user likely authenticates
- Summarize how authorization is enforced on endpoints
- Identify any report-related endpoints that appear weakly protected or unclear

E. Determine data/backend connectivity, especially Snowflake
Search for evidence of:
- Snowflake SDKs, drivers, packages, connectors
- connection strings
- environment variables
- SQL clients
- repository/data-access layers
- ORM usage
- direct SQL execution
- warehouse/database/schema references

Search terms to include:
- snowflake
- snowflake-connector
- SnowflakeDb
- jdbc:snowflake
- odbc
- warehouse
- database
- schema
- query
- execute
- cursor
- select
- from
- sqlalchemy
- Dapper
- EntityFramework
- DbContext
- SqlCommand
- Npgsql
- JdbcTemplate
- repository

Output:
- Does this repo appear to talk directly to Snowflake?
- If yes, where and how?
- If not, is there evidence it calls another internal service that may do so?

F. Determine whether the service is deployable and likely active
Find deployment/build signals such as:
- .gitlab-ci.yml
- included CI files
- Dockerfile
- docker-compose
- Helm charts
- values.yaml
- k8s manifests
- appsettings files
- .env or env templates
- terraform references
- build.gradle, pom.xml, package.json, requirements.txt, pyproject.toml, csproj, sln

Output:
- Is this repo likely a real deployable service?
- What environment or hostname clues exist?
- Any clues about public vs internal exposure?

G. Logging and audit visibility
Search for:
- API request logging
- gateway/correlation IDs
- structured logging
- query logging
- audit trails
- security logging
- exception logging around report execution
- references to Splunk, ELK, App Insights, Log Analytics, Datadog, CloudWatch, etc.

Output:
- Where would I likely look for logs related to suspicious report API calls?
- Does the code log request parameters, user identity, tenant, or query execution?
- Are there any gaps where logs appear insufficient?

H. Security-focused endpoint shortlist
Based on all findings, build a final shortlist of endpoints or handlers that should be tested first for:
- broken object level authorization
- broken function level authorization
- weak tenant scoping
- dangerous filtering/query parameters
- export/download overreach
- injection risk
- hidden/internal endpoints exposed through normal auth

For each shortlisted item include:
- endpoint/path
- why it is risky
- what kind of test should be performed as a normal user

Required final output format:

1. Repo summary
2. Framework detected
3. API inventory table
4. Report/reporting-related findings
5. Swagger/OpenAPI findings
6. Auth/authz findings
7. Data access and Snowflake findings
8. Deployability findings
9. Logging findings
10. High-priority endpoints to test
11. Gaps / unknowns
12. Final risk-focused summary for a security engineer investigating possible sensitive-data exposure via report.api

Important:
- Quote only short relevant snippets, not whole files.
- Always include the file path for each finding.
- If multiple services exist in the repo, separate them.
- If this repo does not appear to contain the API itself, say so clearly and identify what it does appear to contain instead.




----------------------------------------------------------------------------------------------------------------------------

Now validate your own findings.
1. Re-check every conclusion and remove anything speculative.
2. Highlight the top 10 most important files I should inspect manually first.
3. Build a condensed table only for report-related endpoints and data-access code.
4. Tell me whether this repo is:
- definitely hosts the API
- likely hosts the API
- supports the API indirectly
- unrelated
5. Tell me exactly what evidence would confirm Swagger/OpenAPI exists in this repo.
6. Tell me exactly what evidence would confirm a normal user can or cannot access report-related data.



----------------------------------------------------------------------------------------------------------------------------


Search this repository specifically for Swagger/OpenAPI evidence.

Check for:
- swagger.json, swagger.yaml, openapi.json, openapi.yaml
- AddSwaggerGen, UseSwagger, UseSwaggerUI
- springdoc, springfox, @OpenAPIDefinition, @Operation
- swagger-ui-express, swagger-jsdoc, openapi
- FastAPI automatic docs config
- CI/CD steps that generate or publish API specs
- references to /swagger, /openapi, /api-docs

Return:
1. exact file paths
2. short evidence snippets
3. whether the spec is stored as a file, generated by framework, or referenced in pipeline
4. if absent, say “no clear Swagger/OpenAPI evidence found”